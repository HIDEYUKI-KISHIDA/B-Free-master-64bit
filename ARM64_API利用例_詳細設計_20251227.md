# ARM/64 OS API利用例・詳細設計（2025/12/27）

---

## 1. プロセス/スレッドAPI利用例
```c
#include "../kernel_arm/process_arm.c"

void child(void) { /* ... */ }

int main(void) {
    process_init();
    int pid = process_create(child);
    schedule();
}
```

---

## 2. ファイルシステムAPI利用例
```c
#include "../kernel_arm/fs_arm.c"

fs_init();
int fd = fs_open("test.txt");
char buf[64];
fs_read(fd, buf, sizeof(buf));
fs_write(fd, "hello", 5);
fs_close(fd);
```

---

## 3. システムコールAPI利用例
```c
#include "../kernel_arm/syscall_arm.c"

int pid = syscall(0, 0, 0, 0, 0); // getpid
syscall(1, 1, (uintptr_t)"msg", 3, 0); // write
```

---

## 4. 権限管理API利用例
```c
#include "../kernel_arm/security_arm.c"

security_init();
int uid = user_add("user", 1);
int ok = check_access(uid, 0, 4); // 読み取り権限
```

---

## 5. 電源管理API利用例
```c
#include "../kernel_arm/power_arm.c"

power_init();
// power_shutdown();
// power_reboot();
```

---

## 6. UART/TTY API利用例
```c
#include "../kernel_arm/uart_arm.c"

uart_init();
uart_putc('A');
char c = uart_getc();
tty_write("hello\n");
```

---

## 7. GUI/シェルAPI利用例
```c
#include "../kernel_arm/shell_arm.c"

shell_main();
```

---

- 主要APIの呼び出し例・典型的な利用パターンを記載
- サブシステム間の連携や典型フローも随時追記
- 詳細設計・拡張例は今後も追加
