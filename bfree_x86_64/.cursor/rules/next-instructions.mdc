---
description: Read phone/GitHub NEXT_INSTRUCTIONS at session start and follow スマホ指示
alwaysApply: true
---

# NEXT_INSTRUCTIONS (mobile)

**Canonical tree:** `C:\Users\h_kis\Desktop\B-Free-master` only (`docs/CANONICAL.md`). Do not treat `J:\B-Free-master` as current.

At the start of a coding session in this workspace (and whenever the user says「次へ」「NEXT」「指示を見て」or similar):

1. `git fetch origin` (do not merge the whole branch). Read `bfree_x86_64/docs/PHONE.md` from **origin** (`git show origin/HEAD:bfree_x86_64/docs/PHONE.md` or the current tracking branch) first — that is the phone inbox.
2. Also read `docs/NEXT_INSTRUCTIONS.md`.
3. Prefer, in order: (a) origin `PHONE.md` body after the `---` if it is not empty and not only `次へ`/`次`; (b) the local `PHONE.md` the same way; (c) the block between `PHONE_START` and `PHONE_END` in `NEXT_INSTRUCTIONS.md`; (d) the **Next** bullet under「現在の状態」.
4. After completing work, update「現在の状態」and **Next** in `NEXT_INSTRUCTIONS.md`. Reset origin/local `PHONE.md` to a short default like「次へ」only if you finished their phone ask. Leave `PHONE_START`/`PHONE_END` for the user.
5. Do not require the user to paste instructions if these files already have them.
