{
  "meta": {
    "file": "C:\\Users\\h_kis\\.cursor\\projects\\c-Users-h-kis-Desktop-B-Free-master-Program-bfree-x86-64\\agent-transcripts\\f40d0181-b472-47d1-88c0-97da64f8c769\\f40d0181-b472-47d1-88c0-97da64f8c769.jsonl",
    "line": 13,
    "tool": "StrReplace",
    "path": "C:\\Users\\h_kis\\Desktop\\B-Free-master\\Program\\bfree_x86_64\\kernel\\sysmain\\syscall.c",
    "old_len": 1654,
    "new_len": 2271
  },
  "themes": [
    "parked",
    "coop"
  ],
  "old_string": "static long sys_linux_waitid(long idtype, long id, long infop, long options)\n{\n    long pid;\n    int status = 0;\n    int wopts;\n    long rc;\n    bfree_siginfo_wait_t *si;\n\n    if (idtype == BFREE_P_PID) {\n        pid = id;\n    } else if (idtype == BFREE_P_ALL) {\n        pid = -1;\n    } else if (idtype == BFREE_P_PGID) {\n        pid = (id == 0) ? (long)(-g_guest_pgid) : (long)(-id);\n    } else {\n        return -22; /* EINVAL */\n    }\n    /* Require WEXITED for this stub; ignore WSTOPPED/WCONTINUED. */\n    if ((options & BFREE_WEXITED) == 0 && (options & 0x00000002) == 0 &&\n        (options & 0x00000008) == 0) {\n        /* Some callers pass only WNOHANG; treat as wait for exit. */\n    }\n    wopts = ((options & BFREE_WNOHANG_ID) != 0) ? 1 : 0;\n    rc = bfree_process_wait4(pid, &status, wopts);\n    if (rc < 0) {\n        return rc;\n    }\n    if (rc == 0) {\n        return 0; /* WNOHANG, nothing ready */\n    }\n    g_guest_fork_status_ready = 0;\n    if (infop != 0 && bfree_user_ptr_mapped(infop)) {\n        int signaled = (status & 0xff) != 0 && (status & 0xff00) == 0 &&\n                       (status & 0x7f) != 0 && (status & 0x7f) != 0x7f;\n\n        si = (bfree_siginfo_wait_t *)(uintptr_t)infop;\n        si->si_signo = BFREE_SIGCHLD;\n        si->si_errno = 0;\n        if (signaled) {\n            si->si_code = BFREE_CLD_KILLED;\n            si->si_status = status & 0x7f;\n        } else {\n            si->si_code = BFREE_CLD_EXITED;\n            si->si_status = (status >> 8) & 0xff;\n        }\n        si->__pad0 = 0;\n        si->si_pid = (int)rc;\n        si->si_uid = 0;\n        si->si_utime = 0;\n        si->si_stime = 0;\n    }\n    return 0;\n}",
  "new_string": "static long sys_linux_waitid(long idtype, long id, long infop, long options)\n{\n    long pid;\n    int status = 0;\n    int wopts;\n    long rc;\n    bfree_siginfo_wait_t *si;\n\n    if (idtype == BFREE_P_PID) {\n        pid = id;\n    } else if (idtype == BFREE_P_ALL) {\n        pid = -1;\n    } else if (idtype == BFREE_P_PGID) {\n        pid = (id == 0) ? (long)(-g_guest_pgid) : (long)(-id);\n    } else {\n        return -22; /* EINVAL */\n    }\n    /* Require WEXITED for this stub; ignore WSTOPPED/WCONTINUED. */\n    if ((options & BFREE_WEXITED) == 0 && (options & 0x00000002) == 0 &&\n        (options & 0x00000008) == 0) {\n        /* Some callers pass only WNOHANG; treat as wait for exit. */\n    }\n    wopts = ((options & BFREE_WNOHANG_ID) != 0) ? 1 : 0;\n    for (;;) {\n        rc = bfree_process_wait4(pid, &status, wopts);\n        if (rc < 0) {\n            return rc;\n        }\n        if (rc == 0) {\n            if (wopts != 0) {\n                return 0; /* WNOHANG, nothing ready */\n            }\n            /* Blocking: run parked AS-copy child (same as waitpid). */\n            if (g_guest_fork_active && g_coop_side == 0 && g_coop_child_blocked) {\n                g_guest_wait_status_ptr = 0;\n                return bfree_coop_yield_to_child();\n            }\n            {\n                int er = bfree_guest_sig_take_eintr();\n                if (er < 0) {\n                    return er;\n                }\n            }\n            __asm__ volatile(\"sti; hlt\" ::: \"memory\");\n            continue;\n        }\n        break;\n    }\n    g_guest_fork_status_ready = 0;\n    if (infop != 0 && bfree_user_ptr_mapped(infop)) {\n        int signaled = (status & 0xff) != 0 && (status & 0xff00) == 0 &&\n                       (status & 0x7f) != 0 && (status & 0x7f) != 0x7f;\n\n        si = (bfree_siginfo_wait_t *)(uintptr_t)infop;\n        si->si_signo = BFREE_SIGCHLD;\n        si->si_errno = 0;\n        if (signaled) {\n            si->si_code = BFREE_CLD_KILLED;\n            si->si_status = status & 0x7f;\n        } else {\n            si->si_code = BFREE_CLD_EXITED;\n            si->si_status = (status >> 8) & 0xff;\n        }\n        si->__pad0 = 0;\n        si->si_pid = (int)rc;\n        si->si_uid = 0;\n        si->si_utime = 0;\n        si->si_stime = 0;\n    }\n    return 0;\n}"
}