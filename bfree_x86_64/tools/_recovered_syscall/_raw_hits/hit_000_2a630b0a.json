{
  "meta": {
    "file": "C:\\Users\\h_kis\\.cursor\\projects\\c-Users-h-kis-Desktop-B-Free-master-Program-bfree-x86-64\\agent-transcripts\\2a630b0a-aa9d-40d2-9d10-839aec4ecb20\\2a630b0a-aa9d-40d2-9d10-839aec4ecb20.jsonl",
    "line": 1587,
    "tool": "Write",
    "path": "C:\\Users\\h_kis\\Desktop\\B-Free-master\\Program\\bfree_x86_64\\tools\\_patch_stage1_ash.py",
    "old_len": 0,
    "new_len": 21316
  },
  "themes": [
    "parked",
    "unix",
    "coop"
  ],
  "old_string": "",
  "new_string": "#!/usr/bin/env python3\n\"\"\"Stage 1 ash-fatten: cloexec, /home, fork-return-to-parent, stdin/wait yield.\"\"\"\nfrom __future__ import annotations\n\nfrom pathlib import Path\n\nROOT = Path(__file__).resolve().parents[1]\nSC = ROOT / \"kernel\" / \"sysmain\" / \"syscall.c\"\ntext = SC.read_text(encoding=\"utf-8\", errors=\"replace\")\norig = text\n\ndef must_replace(old: str, new: str, label: str) -> None:\n    global text\n    if old not in text:\n        raise SystemExit(f\"missing block: {label}\")\n    text = text.replace(old, new, 1)\n    print(f\"[ok] {label}\")\n\n# --- O_CLOEXEC + cloexec fd bits ---\nmust_replace(\n    \"\"\"#define BFREE_LINUX_O_ACCMODE       3\n#define BFREE_LINUX_O_CREAT         0100\n#define BFREE_LINUX_O_TRUNC         01000\n#define BFREE_LINUX_O_APPEND        02000\n#define BFREE_LINUX_O_DIRECTORY     040000\n\"\"\",\n    \"\"\"#define BFREE_LINUX_O_ACCMODE       3\n#define BFREE_LINUX_O_CREAT         0100\n#define BFREE_LINUX_O_TRUNC         01000\n#define BFREE_LINUX_O_APPEND        02000\n#define BFREE_LINUX_O_DIRECTORY     040000\n#define BFREE_LINUX_O_CLOEXEC       02000000\n#define BFREE_LINUX_O_NONBLOCK      04000\n#define BFREE_LINUX_FD_CLOEXEC      1\n\"\"\",\n    \"O_CLOEXEC defines\",\n)\n\nmust_replace(\n    \"\"\"#define BFREE_GUEST_FD_TABLE_SIZE 64\nstatic int g_guest_fd_target[BFREE_GUEST_FD_TABLE_SIZE];\n/* Resolved target captured at dup(); survives close() until dup2() consumes it. */\nstatic int g_guest_fd_dup_save[BFREE_GUEST_FD_TABLE_SIZE];\nstatic int g_guest_fd_inited;\n\"\"\",\n    \"\"\"#define BFREE_GUEST_FD_TABLE_SIZE 64\nstatic int g_guest_fd_target[BFREE_GUEST_FD_TABLE_SIZE];\n/* Resolved target captured at dup(); survives close() until dup2() consumes it. */\nstatic int g_guest_fd_dup_save[BFREE_GUEST_FD_TABLE_SIZE];\nstatic uint8_t g_guest_fd_cloexec[BFREE_GUEST_FD_TABLE_SIZE];\nstatic int g_guest_fd_inited;\n\"\"\",\n    \"cloexec array\",\n)\n\nmust_replace(\n    \"\"\"    for (i = 0; i < BFREE_GUEST_FD_TABLE_SIZE; ++i) {\n        g_guest_fd_target[i] = -1;\n        g_guest_fd_dup_save[i] = -1;\n    }\n\"\"\",\n    \"\"\"    for (i = 0; i < BFREE_GUEST_FD_TABLE_SIZE; ++i) {\n        g_guest_fd_target[i] = -1;\n        g_guest_fd_dup_save[i] = -1;\n        g_guest_fd_cloexec[i] = 0;\n    }\n\"\"\",\n    \"cloexec init\",\n)\n\n# Forward decl for coop switch constant used early — use literal -4092\n# Patch fork_enter for copy_as parent-first\nmust_replace(\n    \"\"\"    g_guest_fork_pid = child_pid;\n    g_guest_fork_active = 1;\n    g_guest_fork_status_ready = 0;\n    bfree_coop_fd_snap_init();\n    return 0; /* cooperative: continue as child; parent resumes on child exit */\n}\n\"\"\",\n    \"\"\"    g_guest_fork_pid = child_pid;\n    g_guest_fork_active = 1;\n    g_guest_fork_status_ready = 0;\n    bfree_coop_fd_snap_init();\n    if (copy_as) {\n        /*\n         * AS-copy fork: return to parent immediately so `cmd &` / pipelines that\n         * use fork (not vfork) can progress. Child is parked until parent\n         * blocks (stdin/wait/pipe) and yields.\n         */\n        bfree_coop_save_child_user();\n        g_coop_child_blocked = 1;\n        bfree_coop_fd_switch_to(0);\n        g_coop_parent_started = 1;\n        g_bfree_sysret_exec_rsp = g_bfree_fork_saved_rsp;\n        g_bfree_sysret_exec_rcx = g_bfree_fork_saved_rcx;\n        g_bfree_sysret_exec_r11 = g_bfree_fork_saved_r11;\n        g_bfree_fork_parent_ret = (uint64_t)(long)g_guest_fork_pid;\n        return (long)-4092; /* BFREE_SYSRET_COOP_SWITCH */\n    }\n    return 0; /* vfork: continue as child; parent resumes on child exit */\n}\n\"\"\",\n    \"fork parent-first\",\n)\n\n# Need forward decl for bfree_coop_save_child_user and g_coop_parent_started\nmust_replace(\n    \"\"\"static long bfree_coop_yield_to_parent(void);\nstatic long bfree_coop_yield_to_child(void);\nstatic void bfree_coop_fd_snap_init(void);\nstatic void bfree_coop_fd_switch_to(int side);\nstatic int g_coop_side;\nstatic int g_coop_child_blocked;\n\"\"\",\n    \"\"\"static long bfree_coop_yield_to_parent(void);\nstatic long bfree_coop_yield_to_child(void);\nstatic void bfree_coop_fd_snap_init(void);\nstatic void bfree_coop_fd_switch_to(int side);\nstatic void bfree_coop_save_child_user(void);\nstatic int g_coop_side;\nstatic int g_coop_child_blocked;\nstatic int g_coop_parent_started;\n\"\"\",\n    \"coop fwd decls\",\n)\n\n# Remove duplicate g_coop_parent_started later if we add early - check\nif text.count(\"static int g_coop_parent_started;\") > 1:\n    # keep early, remove from unix block\n    text = text.replace(\n        \"static bfree_unix_sock_t g_unix_socks[BFREE_UNIX_SLOTS];\\n\"\n        \"static int g_coop_parent_started;\\n\",\n        \"static bfree_unix_sock_t g_unix_socks[BFREE_UNIX_SLOTS];\\n\",\n        1,\n    )\n    print(\"[ok] dedupe g_coop_parent_started\")\n\n# /home writable — extend path helper after /var block\nold_var = \"\"\"    if (strncmp(path, \"/var/\", 5) == 0 && path[5] != '\\\\0') {\n        size_t n = 4;\n        name_out[0] = 'v';\n        name_out[1] = 'a';\n        name_out[2] = 'r';\n        name_out[3] = '/';\n        while (path[5 + (n - 4)] != '\\\\0' && n + 1U < name_cap) {\n            name_out[n] = path[5 + (n - 4)];\n            ++n;\n        }\n        if (path[5 + (n - 4)] != '\\\\0') {\n            return 0;\n        }\n        name_out[n] = '\\\\0';\n        rel = name_out + 4;\n\"\"\"\n\n# Simpler: after /var == 0 block, add /home == 0 and /home/ handling by duplicating pattern\n# Find parents_exist var special-case and add home\nmust_replace(\n    \"\"\"    /* B-Free: var parent always exists */\n    if (strcmp(parent, \"var\") == 0) {\n        return 1;\n    }\n\"\"\",\n    \"\"\"    /* B-Free: var/home parents always exist */\n    if (strcmp(parent, \"var\") == 0 || strcmp(parent, \"home\") == 0) {\n        return 1;\n    }\n\"\"\",\n    \"parents home\",\n)\n\nmust_replace(\n    \"\"\"    /* B-Free: /var writable namespace */\n    if (strcmp(path, \"/var\") == 0) {\n        if (name_cap < 4) {\n            return 0;\n        }\n        name_out[0] = 'v';\n        name_out[1] = 'a';\n        name_out[2] = 'r';\n        name_out[3] = '\\\\0';\n        return 1;\n    }\n\"\"\",\n    \"\"\"    /* B-Free: /var and /home writable namespaces */\n    if (strcmp(path, \"/var\") == 0) {\n        if (name_cap < 4) {\n            return 0;\n        }\n        name_out[0] = 'v';\n        name_out[1] = 'a';\n        name_out[2] = 'r';\n        name_out[3] = '\\\\0';\n        return 1;\n    }\n    if (strcmp(path, \"/home\") == 0) {\n        if (name_cap < 5) {\n            return 0;\n        }\n        name_out[0] = 'h';\n        name_out[1] = 'o';\n        name_out[2] = 'm';\n        name_out[3] = 'e';\n        name_out[4] = '\\\\0';\n        return 1;\n    }\n\"\"\",\n    \"path /home root\",\n)\n\n# Add /home/ prefix mapping after /var/ block ends - find return 1 before /tmp/\n# Insert after the big /var/ validation return 1\nmarker = \"\"\"        return 1;\n    }\n    if (strncmp(path, \\\"/tmp/\\\", 5) != 0 || path[5] == '\\\\0') {\n\"\"\"\nif marker not in text:\n    # try alternate\n    marker2 = 'if (strncmp(path, \"/tmp/\", 5) != 0 || path[5] == \\'\\\\0\\')'\n    idx = text.find(marker2)\n    if idx < 0:\n        raise SystemExit(\"cannot find /tmp/ check after var\")\n    # find preceding \"return 1;\\n    }\\n\" \n    insert_at = text.rfind(\"return 1;\", 0, idx)\n    # go back to include closing of var block - insert home handler before /tmp check\n    home_block = r'''\n    if (strncmp(path, \"/home/\", 6) == 0 && path[6] != '\\0') {\n        size_t n = 5;\n        name_out[0] = 'h';\n        name_out[1] = 'o';\n        name_out[2] = 'm';\n        name_out[3] = 'e';\n        name_out[4] = '/';\n        while (path[6 + (n - 5)] != '\\0' && n + 1U < name_cap) {\n            name_out[n] = path[6 + (n - 5)];\n            ++n;\n        }\n        if (path[6 + (n - 5)] != '\\0') {\n            return 0;\n        }\n        name_out[n] = '\\0';\n        rel = name_out + 5;\n        seg_start = 0;\n        for (i = 0; rel[i] != '\\0'; ++i) {\n            if (rel[i] == '/') {\n                size_t seglen = i - seg_start;\n                if (seglen == 0) {\n                    return 0;\n                }\n                if (seglen == 1 && rel[seg_start] == '.') {\n                    return 0;\n                }\n                if (seglen == 2 && rel[seg_start] == '.' && rel[seg_start + 1] == '.') {\n                    return 0;\n                }\n                seg_start = i + 1U;\n            }\n        }\n        if (i == 0 || rel[i - 1] == '/') {\n            return 0;\n        }\n        {\n            size_t seglen = i - seg_start;\n            if (seglen == 0) {\n                return 0;\n            }\n            if (seglen == 1 && rel[seg_start] == '.') {\n                return 0;\n            }\n            if (seglen == 2 && rel[seg_start] == '.' && rel[seg_start + 1] == '.') {\n                return 0;\n            }\n        }\n        return 1;\n    }\n'''\n    text = text[:idx] + home_block + \"\\n    \" + text[idx:]\n    print(\"[ok] /home/ path map\")\nelse:\n    print(\"[warn] used alt path for home\")\n\n# HOME_DIR_FD\nmust_replace(\n    \"#define BFREE_GUEST_VAR_DIR_FD      0x3705\\n\",\n    \"#define BFREE_GUEST_VAR_DIR_FD      0x3705\\n\"\n    \"#define BFREE_GUEST_HOME_DIR_FD     0x3732\\n\",\n    \"HOME_DIR_FD\",\n)\n\n# openat for /home\nmust_replace(\n    \"\"\"    if (strcmp(path, \"/var\") == 0 || (want_dir && strncmp(path, \"/var/\", 5) == 0 && path[5] == '\\\\0')) {\n        return bfree_guest_vfile_publish_open(BFREE_GUEST_VAR_DIR_FD, (int)flags, 0);\n    }\n\"\"\",\n    \"\"\"    if (strcmp(path, \"/var\") == 0 || (want_dir && strncmp(path, \"/var/\", 5) == 0 && path[5] == '\\\\0')) {\n        return bfree_guest_vfile_publish_open(BFREE_GUEST_VAR_DIR_FD, (int)flags, 0);\n    }\n    if (strcmp(path, \"/home\") == 0 || (want_dir && strncmp(path, \"/home/\", 6) == 0 && path[6] == '\\\\0')) {\n        return bfree_guest_vfile_publish_open(BFREE_GUEST_HOME_DIR_FD, (int)flags, 0);\n    }\n\"\"\",\n    \"openat /home\",\n)\n\n# fcntl real cloexec + setfl\nmust_replace(\n    \"\"\"    case 1: /* F_GETFD */\n        return 0;\n    case 3: /* F_GETFL */\n        if (resolved == 1 || resolved == 2) {\n            return 1;\n        }\n        if (bfree_guest_ofd_from_fd(resolved)) {\n            return bfree_guest_ofd_from_fd(resolved)->flags;\n        }\n        if (bfree_guest_is_vfile_fd(resolved)) {\n            return 1;\n        }\n        return 0;\n    default:\n        return 0;\n    }\n}\n\"\"\",\n    \"\"\"    case 1: /* F_GETFD */\n        if (fd >= 0 && fd < BFREE_GUEST_FD_TABLE_SIZE && g_guest_fd_cloexec[fd]) {\n            return BFREE_LINUX_FD_CLOEXEC;\n        }\n        return 0;\n    case 2: /* F_SETFD */\n        if (fd >= 0 && fd < BFREE_GUEST_FD_TABLE_SIZE) {\n            g_guest_fd_cloexec[fd] = ((arg & BFREE_LINUX_FD_CLOEXEC) != 0) ? 1 : 0;\n        }\n        return 0;\n    case 3: /* F_GETFL */\n        if (resolved == 1 || resolved == 2) {\n            return 1;\n        }\n        if (bfree_guest_ofd_from_fd(resolved)) {\n            return bfree_guest_ofd_from_fd(resolved)->flags;\n        }\n        if (bfree_guest_is_vfile_fd(resolved)) {\n            return 1;\n        }\n        return 0;\n    case 4: /* F_SETFL */\n        {\n            bfree_guest_ofd_t *ofd = bfree_guest_ofd_from_fd(resolved);\n            int fl = (int)arg;\n            if (ofd) {\n                ofd->flags = (ofd->flags & (BFREE_LINUX_O_ACCMODE | BFREE_LINUX_O_APPEND |\n                             BFREE_LINUX_O_DIRECTORY | BFREE_LINUX_O_CLOEXEC)) |\n                             (fl & (BFREE_LINUX_O_APPEND | BFREE_LINUX_O_NONBLOCK));\n            }\n            if (bfree_guest_pipe_slot_from_magic(resolved) >= 0) {\n                int slot = bfree_guest_pipe_slot_from_magic(resolved);\n                if (slot >= 0) {\n                    g_guest_pipes[slot].nonblock = (fl & BFREE_LINUX_O_NONBLOCK) != 0;\n                }\n            }\n        }\n        return 0;\n    default:\n        return 0;\n    }\n}\n\"\"\",\n    \"fcntl cloexec/setfl\",\n)\n\nmust_replace(\n    \"\"\"static long sys_linux_dup3(long oldfd, long newfd, long flags)\n{\n    (void)flags;\n    return sys_linux_dup2(oldfd, newfd);\n}\n\"\"\",\n    \"\"\"static long sys_linux_dup3(long oldfd, long newfd, long flags)\n{\n    long rc = sys_linux_dup2(oldfd, newfd);\n    if (rc >= 0 && newfd >= 0 && newfd < BFREE_GUEST_FD_TABLE_SIZE) {\n        g_guest_fd_cloexec[(int)newfd] =\n            ((flags & (long)BFREE_LINUX_O_CLOEXEC) != 0) ? 1 : 0;\n    }\n    return rc;\n}\n\"\"\",\n    \"dup3 cloexec\",\n)\n\n# openat: set cloexec on publish — find bfree_guest_vfile_publish_open returns\n# Better: wrap at end of openat success paths is hard. Patch publish_open and fd_publish.\n\n# Add helper after fd_ensure_init area - patch publish to accept cloexec via flags on OFD\n# And after openat returns, set cloexec from flags - patch openat's return sites is many.\n\n# Simpler: in bfree_guest_fd_publish, don't know flags. In openat after computing return:\n# Actually patch sys_linux_openat to set cloexec when return >= 0 && < TABLE\n\n# Find end of sys_linux_openat - the function is huge. Add wrapper:\n# static long sys_linux_openat(...) { long rc = ...; if (rc>=0 && rc < SIZE && flags&CLOEXEC) cloexec[rc]=1; return rc; }\n# Too invasive.\n\n# Patch bfree_guest_vfile_publish_open to take cloexec from flags:\nmust_replace(\n    \"\"\"            published = bfree_guest_fd_publish(BFREE_GUEST_OFD_FD_BASE + i);\n            if (published < 0) {\n                memset(&g_guest_ofds[i], 0, sizeof(g_guest_ofds[i]));\n            } else {\n                bfree_guest_vfile_ref(target, 1);\n            }\n            return published;\n\"\"\",\n    \"\"\"            published = bfree_guest_fd_publish(BFREE_GUEST_OFD_FD_BASE + i);\n            if (published < 0) {\n                memset(&g_guest_ofds[i], 0, sizeof(g_guest_ofds[i]));\n            } else {\n                bfree_guest_vfile_ref(target, 1);\n                if (published >= 0 && published < BFREE_GUEST_FD_TABLE_SIZE) {\n                    g_guest_fd_cloexec[published] =\n                        ((flags & (int)BFREE_LINUX_O_CLOEXEC) != 0) ? 1 : 0;\n                }\n            }\n            return published;\n\"\"\",\n    \"publish cloexec\",\n)\n\n# pipe2 cloexec\nmust_replace(\n            \"\"\"            pipefd[0] = bfree_guest_fd_publish(rd);\n            pipefd[1] = bfree_guest_fd_publish(wr);\n            if (pipefd[0] < 0 || pipefd[1] < 0) {\n                g_guest_pipes[i].used = 0;\n                return -24;\n            }\n            return 0;\n\"\"\",\n            \"\"\"            pipefd[0] = bfree_guest_fd_publish(rd);\n            pipefd[1] = bfree_guest_fd_publish(wr);\n            if (pipefd[0] < 0 || pipefd[1] < 0) {\n                g_guest_pipes[i].used = 0;\n                return -24;\n            }\n            if (((unsigned long)flags & (unsigned long)BFREE_LINUX_O_CLOEXEC) != 0UL) {\n                if (pipefd[0] < BFREE_GUEST_FD_TABLE_SIZE) {\n                    g_guest_fd_cloexec[pipefd[0]] = 1;\n                }\n                if (pipefd[1] < BFREE_GUEST_FD_TABLE_SIZE) {\n                    g_guest_fd_cloexec[pipefd[1]] = 1;\n                }\n            }\n            return 0;\n\"\"\",\n            \"pipe2 cloexec\",\n)\n\n# execve close-on-exec: after maps reset, before load\nmust_replace(\n    \"\"\"        bfree_guest_proc_maps_select_busybox(is_p8 ? 0 : 1);\n\n        if (is_child) {\n\"\"\",\n    \"\"\"        bfree_guest_proc_maps_select_busybox(is_p8 ? 0 : 1);\n        /* close-on-exec */\n        {\n            int fdi;\n            for (fdi = 3; fdi < BFREE_GUEST_FD_TABLE_SIZE; ++fdi) {\n                if (g_guest_fd_cloexec[fdi]) {\n                    (void)sys_linux_close(fdi);\n                    g_guest_fd_cloexec[fdi] = 0;\n                }\n            }\n        }\n\n        if (is_child) {\n\"\"\",\n    \"exec cloexec\",\n)\n\n# Note: sys_linux_close must be declared/defined before execve - it is.\n\n# stdin yield when parent and child blocked - find bfree_stdin blocking\n# Search for sti; hlt in stdin path\nif \"g_guest_fork_active && g_coop_side == 0 && g_coop_child_blocked\" not in text[text.find(\"bfree_stdin_read_user\"):text.find(\"bfree_stdin_read_user\")+3000]:\n    # find a hlt in stdin read loop\n    pass\n\n# Find waitpid and add yield\nwait_marker = \"static long sys_linux_waitpid\"\nwi = text.find(wait_marker)\nif wi > 0:\n    # find first blocking loop or WNOHANG check\n    print(\"[info] waitpid at\", wi)\n\n# Snap cloexec in fd switch\nmust_replace(\n    \"\"\"static void bfree_coop_fd_switch_to(int side)\n{\n    int i;\n    if (side == g_coop_side) {\n        return;\n    }\n    if (g_coop_side == 1) {\n        for (i = 0; i < BFREE_GUEST_FD_TABLE_SIZE; ++i) {\n            g_fd_snap_child[i] = g_guest_fd_target[i];\n        }\n        for (i = 0; i < BFREE_GUEST_FD_TABLE_SIZE; ++i) {\n            g_guest_fd_target[i] = g_fd_snap_parent[i];\n        }\n    } else {\n        for (i = 0; i < BFREE_GUEST_FD_TABLE_SIZE; ++i) {\n            g_fd_snap_parent[i] = g_guest_fd_target[i];\n        }\n        for (i = 0; i < BFREE_GUEST_FD_TABLE_SIZE; ++i) {\n            g_guest_fd_target[i] = g_fd_snap_child[i];\n        }\n    }\n    g_coop_side = side;\n}\n\"\"\",\n    \"\"\"static uint8_t g_fd_cloexec_snap_parent[BFREE_GUEST_FD_TABLE_SIZE];\nstatic uint8_t g_fd_cloexec_snap_child[BFREE_GUEST_FD_TABLE_SIZE];\n\nstatic void bfree_coop_fd_switch_to(int side)\n{\n    int i;\n    if (side == g_coop_side) {\n        return;\n    }\n    if (g_coop_side == 1) {\n        for (i = 0; i < BFREE_GUEST_FD_TABLE_SIZE; ++i) {\n            g_fd_snap_child[i] = g_guest_fd_target[i];\n            g_fd_cloexec_snap_child[i] = g_guest_fd_cloexec[i];\n        }\n        for (i = 0; i < BFREE_GUEST_FD_TABLE_SIZE; ++i) {\n            g_guest_fd_target[i] = g_fd_snap_parent[i];\n            g_guest_fd_cloexec[i] = g_fd_cloexec_snap_parent[i];\n        }\n    } else {\n        for (i = 0; i < BFREE_GUEST_FD_TABLE_SIZE; ++i) {\n            g_fd_snap_parent[i] = g_guest_fd_target[i];\n            g_fd_cloexec_snap_parent[i] = g_guest_fd_cloexec[i];\n        }\n        for (i = 0; i < BFREE_GUEST_FD_TABLE_SIZE; ++i) {\n            g_guest_fd_target[i] = g_fd_snap_child[i];\n            g_guest_fd_cloexec[i] = g_fd_cloexec_snap_child[i];\n        }\n    }\n    g_coop_side = side;\n}\n\"\"\",\n    \"fd switch cloexec snap\",\n)\n\nmust_replace(\n    \"\"\"    for (i = 0; i < BFREE_GUEST_FD_TABLE_SIZE; ++i) {\n        g_fd_snap_parent[i] = g_guest_fd_target[i];\n        g_fd_snap_child[i] = g_guest_fd_target[i];\n    }\n    g_coop_side = 1;\n\"\"\",\n    \"\"\"    for (i = 0; i < BFREE_GUEST_FD_TABLE_SIZE; ++i) {\n        g_fd_snap_parent[i] = g_guest_fd_target[i];\n        g_fd_snap_child[i] = g_guest_fd_target[i];\n        g_fd_cloexec_snap_parent[i] = g_guest_fd_cloexec[i];\n        g_fd_cloexec_snap_child[i] = g_guest_fd_cloexec[i];\n    }\n    g_coop_side = 1;\n\"\"\",\n    \"snap init cloexec\",\n)\n\n# Problem: g_fd_cloexec_snap_* used in snap_init before defined if snap_init is before switch.\n# Move arrays next to g_fd_snap_parent\n\n# Fix order - I inserted snap arrays inside switch function area but snap_init is before switch.\n# Move arrays to be with g_fd_snap_parent\ntext = text.replace(\n    \"\"\"static uint8_t g_fd_cloexec_snap_parent[BFREE_GUEST_FD_TABLE_SIZE];\nstatic uint8_t g_fd_cloexec_snap_child[BFREE_GUEST_FD_TABLE_SIZE];\n\nstatic void bfree_coop_fd_switch_to(int side)\n\"\"\",\n    \"\"\"static void bfree_coop_fd_switch_to(int side)\n\"\"\",\n    1,\n)\nmust_replace(\n    \"\"\"static int g_fd_snap_parent[BFREE_GUEST_FD_TABLE_SIZE];\nstatic int g_fd_snap_child[BFREE_GUEST_FD_TABLE_SIZE];\n\"\"\",\n    \"\"\"static int g_fd_snap_parent[BFREE_GUEST_FD_TABLE_SIZE];\nstatic int g_fd_snap_child[BFREE_GUEST_FD_TABLE_SIZE];\nstatic uint8_t g_fd_cloexec_snap_parent[BFREE_GUEST_FD_TABLE_SIZE];\nstatic uint8_t g_fd_cloexec_snap_child[BFREE_GUEST_FD_TABLE_SIZE];\n\"\"\",\n    \"cloexec snap near fd snap\",\n)\n\n# root getdents add home\nmust_replace(\n    \"\"\"        \".\", \"..\", \"bin\", \"dev\", \"etc\", \"proc\", \"root\", \"tmp\", \"usr\", \"var\",\n        \"busybox.elf\", \"p8test.elf\"\n\"\"\",\n    \"\"\"        \".\", \"..\", \"bin\", \"dev\", \"etc\", \"home\", \"proc\", \"root\", \"tmp\", \"usr\", \"var\",\n        \"busybox.elf\", \"p8test.elf\"\n\"\"\",\n    \"readdir home\",\n)\n\n# access/stat for /home - add near /var checks\nmust_replace(\n    \"\"\"    if (strcmp(path, \"/var\") == 0 || strncmp(path, \"/var/\", 4) == 0) {\n        return 0;\n    }\n\"\"\",\n    \"\"\"    if (strcmp(path, \"/var\") == 0 || strncmp(path, \"/var/\", 4) == 0) {\n        return 0;\n    }\n    if (strcmp(path, \"/home\") == 0 || strncmp(path, \"/home/\", 6) == 0) {\n        return 0;\n    }\n\"\"\",\n    \"access home\",\n)\n\nmust_replace(\n    \"\"\"    if (strcmp(path, \"/var\") == 0) {\n        return bfree_linux_stat_fill(statbuf, BFREE_LINUX_S_IFDIR | 0755U, 4096);\n    }\n\"\"\",\n    \"\"\"    if (strcmp(path, \"/var\") == 0) {\n        return bfree_linux_stat_fill(statbuf, BFREE_LINUX_S_IFDIR | 0755U, 4096);\n    }\n    if (strcmp(path, \"/home\") == 0) {\n        return bfree_linux_stat_fill(statbuf, BFREE_LINUX_S_IFDIR | 0755U, 4096);\n    }\n\"\"\",\n    \"stat home\",\n)\n\n# DIR fd checks that mention VAR_DIR - add HOME\nfor old, new, lab in [\n    (\n        \"target == (int)BFREE_GUEST_VAR_DIR_FD || target == (int)BFREE_GUEST_PROC_DIR_FD ||\",\n        \"target == (int)BFREE_GUEST_VAR_DIR_FD || target == (int)BFREE_GUEST_HOME_DIR_FD || \"\n        \"target == (int)BFREE_GUEST_PROC_DIR_FD ||\",\n        \"dirfd home a\",\n    ),\n]:\n    if old in text:\n        text = text.replace(old, new)\n        print(f\"[ok] {lab}\")\n\n# getdents for HOME_DIR - find VAR_DIR_FD handling\nif \"BFREE_GUEST_VAR_DIR_FD\" in text and \"BFREE_GUEST_HOME_DIR_FD\" in text:\n    # add k_home_names similar to var if there's a var-only branch\n    pass\n\nif text == orig:\n    raise SystemExit(\"no changes?\")\nSC.write_text(text, encoding=\"utf-8\")\nprint(f\"wrote {SC} delta={len(text)-len(orig)}\")\n"
}