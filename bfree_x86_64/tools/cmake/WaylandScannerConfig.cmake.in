# Generated by B-Free install_wayland_cmake_configs.sh — host wayland-scanner for guest QtWayland.
include(FindPackageHandleStandardArgs)

set(WaylandScanner_EXECUTABLE "@SCANNER@")
file(TO_CMAKE_PATH "${WaylandScanner_EXECUTABLE}" WaylandScanner_EXECUTABLE)

if(NOT TARGET Wayland::Scanner)
  add_executable(Wayland::Scanner IMPORTED)
  set_target_properties(Wayland::Scanner PROPERTIES
    IMPORTED_LOCATION "${WaylandScanner_EXECUTABLE}"
  )
endif()

find_package_handle_standard_args(WaylandScanner
  FOUND_VAR WaylandScanner_FOUND
  REQUIRED_VARS WaylandScanner_EXECUTABLE
)
