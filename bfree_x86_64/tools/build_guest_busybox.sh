#!/usr/bin/env bash
# Build guest BusyBox userspace (requires local toolchain + BusyBox tree).
set -euo pipefail

echo "build_guest_busybox: stub — integrate with local BusyBox tree on host." >&2
echo "See docs/POSIX_FULL_COMPAT_ROADMAP.ja.md M3 for NOFORK rollback plan." >&2
exit 0
